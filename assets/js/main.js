(function() {
  $(document).ready(function() {
    "use strict"; var window_width = $(window).width(), window_height = window.innerHeight, header_height = $(".default-header").height(), header_height_static = $(".site-header.static").outerHeight(), fitscreen = window_height - header_height; $(".fullscreen").css("height", window_height)
    $(".fitscreen").css("height", fitscreen); if (document.getElementById("default-select")) { $('select').niceSelect() }; $(window).scroll(function() { if ($(this).scrollTop() > 100) { $('#header').addClass('header-scrolled') } else { $('#header').removeClass('header-scrolled') } }); $(".hover").mouseleave(function() { $(this).removeClass("hover") })
  }); function initMenu() {
    try {
      var btns = Array.prototype.slice.call(document.querySelectorAll('.navbar-burger')); var nav = document.getElementById('nav-menu-container'); if (!nav || btns.length === 0) { var pollKey = '__astroNavPoll'; if (!window[pollKey]) window[pollKey] = { attempts: 0, timer: null }; if (window[pollKey].attempts >= 20) return; window[pollKey].attempts += 1; clearTimeout(window[pollKey].timer); window[pollKey].timer = setTimeout(function() { initMenu() }, 150); return }
      if (window.__astroNavPoll) { clearTimeout(window.__astroNavPoll.timer); window.__astroNavPoll = null }
      if (nav.dataset.menuInit === '1') return; if (nav.hasAttribute('data-cloak')) nav.removeAttribute('data-cloak'); var overlay = document.getElementById('mobile-body-overly'); if (!overlay) { overlay = document.createElement('div'); overlay.id = 'mobile-body-overly'; overlay.style.display = 'none'; document.body.appendChild(overlay) }
      function closeAll() { nav.classList.remove('open'); overlay.style.display = 'none'; document.body.style.overflow = ''; btns.forEach(function(b) { b.classList.remove('is-active'); b.setAttribute('aria-expanded', 'false') }) }
      overlay.addEventListener('click', closeAll); btns.forEach(function(btn) { btn.setAttribute('aria-expanded', 'false'); btn.setAttribute('aria-label', 'Open navigatie'); btn.addEventListener('click', function(e) { e.preventDefault(); var isOpen = nav.classList.toggle('open'); btn.classList.toggle('is-active', isOpen); btn.setAttribute('aria-expanded', isOpen ? 'true' : 'false'); btn.setAttribute('aria-label', isOpen ? 'Sluit navigatie' : 'Open navigatie'); overlay.style.display = isOpen ? 'block' : 'none'; document.body.style.overflow = isOpen ? 'hidden' : '' }) }); if (!window.__astroNavEscAdded) { document.addEventListener('keydown', function(e) { if (e.key === 'Escape') closeAll(); }); window.__astroNavEscAdded = !0 }
      nav.dataset.menuInit = '1'
    } catch (err) { console.error('initMenu error', err) }
  } (function() { if (!window.__astroNavObserverAdded) { var body = document.body; if (!body) return; var obs = new MutationObserver(function(muts) { muts.forEach(function(mu) { if (mu.addedNodes && mu.addedNodes.length) { for (var i = 0; i < mu.addedNodes.length; i++) { var n = mu.addedNodes[i]; if (n.nodeType === 1) { if (n.id === 'nav-menu-container' || n.querySelector && n.querySelector('#nav-menu-container')) { setTimeout(initMenu, 50); return } } } } }) }); obs.observe(document.documentElement || document.body, { childList: !0, subtree: !0 }); window.__astroNavObserverAdded = !0 } })(); function scheduleInit() { initMenu();[100, 300, 800].forEach(function(ms) { setTimeout(initMenu, ms) }) }
  document.addEventListener('DOMContentLoaded', scheduleInit); document.addEventListener('astro:page-load', function() { if (document.getElementById('nav-menu-container')) document.getElementById('nav-menu-container').dataset.menuInit = ''; scheduleInit() }); window.addEventListener('load', scheduleInit); window.addEventListener('resize', function() { setTimeout(initMenu, 100) }); scheduleInit()
})()
